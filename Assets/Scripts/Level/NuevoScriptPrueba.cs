using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NuevoScriptPrueba : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Prueba cambiado en nano
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
